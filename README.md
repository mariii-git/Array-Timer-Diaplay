# Array Timer Display

A WinForms application that displays array elements sequentially using a timer.

## Features
- Displays characters from an array one by one
- Timer-controlled display with 1-second intervals
- Simple and intuitive interface

## Technologies
- C#
- WinForms
- Timer Control

## Usage
Click the button to start the timer. The label will show each character from the array every second.
